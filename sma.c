#include<stdio.h>

int main(){

  int arr[50] = {10, 20, 30, 40, 50};
  length = sizeof(arr)/sizeof(arr[0]);
  int i;
  for(i = 0; i < length; ++i)
  {
    printf("%d ", arr[i];
  }
  return 0;
}

  
